node_modules
.next
out
.env
.env.local
*.log
.DS_Store

# Generated by scripts/write-icons.js from scripts/icon-data.json on every
# build (see package.json "prebuild") — not committed directly since PNG
# bytes can't survive the API text-content path used to push this repo.
public/icons/*.png
public/apple-touch-icon.png
